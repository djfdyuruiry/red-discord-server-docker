#! /usr/bin/env bash
set -Eeuo pipefail

function main() {
  ln -fs "/usr/share/zoneinfo/${TZ}" /etc/localtime

  usermod -u ${USER_ID} red
  groupmod -g ${GROUP_ID} red

  chown -R red:red /home/red 

  su red -s ./bin/start-red
}

main
